def printvar(var_name, var_val):
    print(f"------------{var_name.upper()}------------")
    print(var_val)
    print("\n\n\n")

# ----------- how to use ----------
# from app.printvar import printvar
# printvar("schema", schema)
# printvar("add_fields", add_fields)
# printvar("edit_fields", edit_fields)
# printvar("view_fields", view_fields)
